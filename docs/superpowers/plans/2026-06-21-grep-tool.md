# F08 Grep 工具实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现一个对齐 Claude Code 的 Grep 工具，用于在代码库中搜索匹配指定模式的内容

**Architecture:** 封装系统的 ripgrep 命令，通过 subprocess 调用并解析输出，支持正则表达式、文件过滤、路径指定等常用参数

**Tech Stack:** Python 3.12+, subprocess, shutil, os, pytest

## Global Constraints

- Python 3.12+，使用类型注解
- 对齐 Claude Code 的 Grep 工具行为
- 所有函数必须有完整类型注解和 docstring
- 错误信息要有上下文，不吞异常
- 测试覆盖正常路径和错误路径
- 通过 `make check` 验证

---

## 文件结构

```
src/agent/tools/grep.py       # 主实现文件（创建）
tests/tools/test_grep.py      # 测试文件（创建）
specs/F08-grep-tool.md        # spec 文件（从设计文档复制）
```

---

### Task 1: 创建基础结构和参数定义

**Files:**
- Create: `src/agent/tools/grep.py`
- Create: `tests/tools/test_grep.py`

**Interfaces:**
- Consumes: `build_tool` from `agent.tools.base`, `ToolResult`, `ValidationResult` from `agent.core.types`, `ToolUseContext` from `agent.core.context`
- Produces: `grep_tool` (ToolImpl), `execute_grep`, `validate_grep_input`

- [ ] **Step 1: 创建 grep.py 基础结构**

```python
"""搜索工具 - 让 Agent 能够搜索文件内容

对齐 Claude Code 的 Grep 工具实现。
使用 ripgrep 作为底层搜索引擎。
"""

from __future__ import annotations

import os
import shutil
import subprocess
from typing import Any

from agent.core.context import ToolUseContext
from agent.core.types import ToolResult, ValidationResult
from agent.tools.base import build_tool


# ============================================================
# 常量
# ============================================================

DEFAULT_MAX_RESULTS = 100


# ============================================================
# 辅助函数
# ============================================================


def _check_ripgrep_installed() -> bool:
    """检查 ripgrep 是否已安装

    Returns:
        True 如果 ripgrep 已安装，否则 False
    """
    return shutil.which("rg") is not None


def _resolve_path(path: str, cwd: str) -> str:
    """解析路径为绝对路径

    Args:
        path: 输入路径（绝对或相对）
        cwd: 当前工作目录

    Returns:
        绝对路径字符串
    """
    if os.path.isabs(path):
        return path
    return os.path.abspath(os.path.join(cwd, path))


# ============================================================
# 参数定义
# ============================================================

GREP_PARAMETERS = {
    "type": "object",
    "properties": {
        "pattern": {
            "type": "string",
            "description": "搜索模式（支持正则表达式）",
        },
        "path": {
            "type": "string",
            "description": "搜索路径（绝对或相对路径），默认整个项目",
        },
        "include": {
            "type": "string",
            "description": "文件过滤（glob 模式，如 *.py、*.ts）",
        },
        "max_results": {
            "type": "integer",
            "description": "最大结果数，默认 100",
            "default": DEFAULT_MAX_RESULTS,
        },
        "case_sensitive": {
            "type": "boolean",
            "description": "大小写敏感，默认 false",
            "default": False,
        },
        "context_lines": {
            "type": "integer",
            "description": "显示匹配行前后的上下文行数，默认 0",
            "default": 0,
        },
    },
    "required": ["pattern"],
}
```

- [ ] **Step 2: 创建测试文件基础结构**

```python
"""Grep 工具测试"""

from __future__ import annotations

import os
import pytest
from unittest.mock import patch, MagicMock

from agent.core.context import ToolUseContext
from agent.tools.grep import (
    _check_ripgrep_installed,
    _resolve_path,
    execute_grep,
    validate_grep_input,
    GREP_PARAMETERS,
)
```

- [ ] **Step 3: 运行测试确认基础结构正确**

Run: `python -m pytest tests/tools/test_grep.py -v`
Expected: 测试文件能正常导入，无语法错误

- [ ] **Step 4: 提交**

```bash
git add src/agent/tools/grep.py tests/tools/test_grep.py
git commit -m "feat(F08): create grep.py base structure with parameters"
```

---

### Task 2: 实现辅助函数

**Files:**
- Modify: `src/agent/tools/grep.py`
- Modify: `tests/tools/test_grep.py`

**Interfaces:**
- Consumes: `_check_ripgrep_installed`, `_resolve_path` from Task 1
- Produces: `_build_rg_command`, `_parse_rg_output`

- [ ] **Step 1: 编写 _build_rg_command 测试**

```python
def test_build_rg_command_basic():
    """测试基本的命令构造"""
    from agent.tools.grep import _build_rg_command
    cmd = _build_rg_command(
        pattern="TODO",
        path="src/",
        include=None,
        max_results=100,
        case_sensitive=False,
        context_lines=0,
    )
    assert "rg" in cmd
    assert "--line-number" in cmd
    assert "--with-filename" in cmd
    assert "--no-heading" in cmd
    assert "TODO" in cmd
    assert "src/" in cmd


def test_build_rg_command_with_include():
    """测试带文件过滤的命令构造"""
    from agent.tools.grep import _build_rg_command
    cmd = _build_rg_command(
        pattern="TODO",
        path="src/",
        include="*.py",
        max_results=100,
        case_sensitive=False,
        context_lines=0,
    )
    assert "--glob" in cmd
    assert "*.py" in cmd


def test_build_rg_command_case_sensitive():
    """测试大小写敏感的命令构造"""
    from agent.tools.grep import _build_rg_command
    cmd = _build_rg_command(
        pattern="TODO",
        path="src/",
        include=None,
        max_results=100,
        case_sensitive=True,
        context_lines=0,
    )
    assert "--case-sensitive" in cmd


def test_build_rg_command_context_lines():
    """测试带上下文的命令构造"""
    from agent.tools.grep import _build_rg_command
    cmd = _build_rg_command(
        pattern="TODO",
        path="src/",
        include=None,
        max_results=100,
        case_sensitive=False,
        context_lines=2,
    )
    assert "--context" in cmd
    assert "2" in cmd
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/tools/test_grep.py::test_build_rg_command_basic -v`
Expected: FAIL with "ImportError: cannot import name '_build_rg_command'"

- [ ] **Step 3: 实现 _build_rg_command**

```python
def _build_rg_command(
    pattern: str,
    path: str,
    include: str | None,
    max_results: int,
    case_sensitive: bool,
    context_lines: int,
) -> list[str]:
    """构造 ripgrep 命令参数

    Args:
        pattern: 搜索模式（正则）
        path: 搜索路径
        include: 文件过滤（glob 模式）
        max_results: 最大结果数
        case_sensitive: 大小写敏感
        context_lines: 上下文行数

    Returns:
        命令参数列表
    """
    cmd = [
        "rg",
        "--line-number",
        "--with-filename",
        "--no-heading",
        "--max-count",
        str(max_results),
    ]

    if not case_sensitive:
        cmd.append("--case-insensitive")

    if include:
        cmd.extend(["--glob", include])

    if context_lines > 0:
        cmd.extend(["--context", str(context_lines)])

    cmd.append(pattern)
    cmd.append(path)

    return cmd
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/tools/test_grep.py::test_build_rg_command_basic -v`
Expected: PASS

- [ ] **Step 5: 编写 _parse_rg_output 测试**

```python
def test_parse_rg_output_basic():
    """测试基本的输出解析"""
    from agent.tools.grep import _parse_rg_output
    output = "src/main.py:15:# TODO: implement this\nsrc/utils.py:42:# TODO: fix bug\n"
    results = _parse_rg_output(output)
    assert len(results) == 2
    assert results[0]["file"] == "src/main.py"
    assert results[0]["line"] == 15
    assert results[0]["content"] == "# TODO: implement this"
    assert results[1]["file"] == "src/utils.py"
    assert results[1]["line"] == 42
    assert results[1]["content"] == "# TODO: fix bug"


def test_parse_rg_output_empty():
    """测试空输出"""
    from agent.tools.grep import _parse_rg_output
    output = ""
    results = _parse_rg_output(output)
    assert len(results) == 0


def test_parse_rg_output_no_match():
    """测试无匹配结果"""
    from agent.tools.grep import _parse_rg_output
    output = ""
    results = _parse_rg_output(output)
    assert len(results) == 0
```

- [ ] **Step 6: 运行测试确认失败**

Run: `python -m pytest tests/tools/test_grep.py::test_parse_rg_output_basic -v`
Expected: FAIL with "ImportError: cannot import name '_parse_rg_output'"

- [ ] **Step 7: 实现 _parse_rg_output**

```python
def _parse_rg_output(output: str) -> list[dict[str, Any]]:
    """解析 ripgrep 输出

    Args:
        output: ripgrep 的原始输出

    Returns:
        解析后的结果列表，每个结果包含 file、line、content
    """
    results = []
    for line in output.strip().split("\n"):
        if not line:
            continue
        # 格式：文件名:行号:内容
        parts = line.split(":", 2)
        if len(parts) == 3:
            try:
                results.append({
                    "file": parts[0],
                    "line": int(parts[1]),
                    "content": parts[2],
                })
            except ValueError:
                # 行号解析失败，跳过该行
                continue
    return results
```

- [ ] **Step 8: 运行测试确认通过**

Run: `python -m pytest tests/tools/test_grep.py::test_parse_rg_output_basic -v`
Expected: PASS

- [ ] **Step 9: 提交**

```bash
git add src/agent/tools/grep.py tests/tools/test_grep.py
git commit -m "feat(F08): implement helper functions for command construction and output parsing"
```

---

### Task 3: 实现 execute_grep 核心逻辑

**Files:**
- Modify: `src/agent/tools/grep.py`
- Modify: `tests/tools/test_grep.py`

**Interfaces:**
- Consumes: `_check_ripgrep_installed`, `_resolve_path`, `_build_rg_command`, `_parse_rg_output` from Task 1-2
- Produces: `execute_grep`

- [ ] **Step 1: 编写 execute_grep 测试（ripgrep 未安装）**

```python
@patch("agent.tools.grep._check_ripgrep_installed", return_value=False)
def test_execute_grep_ripgrep_not_installed(mock_check):
    """测试 ripgrep 未安装时的错误处理"""
    context = MagicMock(spec=ToolUseContext)
    context.cwd = "/test"
    context.abort_controller.is_aborted = False

    result = execute_grep({"pattern": "TODO"}, context)
    assert result.is_error is True
    assert "ripgrep" in result.output
    assert "安装" in result.output
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/tools/test_grep.py::test_execute_grep_ripgrep_not_installed -v`
Expected: FAIL with "ImportError: cannot import name 'execute_grep'"

- [ ] **Step 3: 实现 execute_grep（ripgrep 未安装部分）**

```python
def execute_grep(input: dict[str, Any], context: ToolUseContext) -> ToolResult:
    """执行 grep 搜索

    Args:
        input: 工具输入参数
        context: 工具执行上下文

    Returns:
        ToolResult: 搜索结果
    """
    # 检查 ripgrep 是否安装
    if not _check_ripgrep_installed():
        return ToolResult(
            output="ripgrep 未安装。请安装 ripgrep：\n"
                   "  Windows: winget install BurntSushi.ripgrep.MSVC\n"
                   "  Mac: brew install ripgrep\n"
                   "  Linux: sudo apt install ripgrep",
            is_error=True,
        )

    # TODO: 继续实现其他逻辑
    return ToolResult(output="Not implemented yet", is_error=True)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/tools/test_grep.py::test_execute_grep_ripgrep_not_installed -v`
Expected: PASS

- [ ] **Step 5: 编写 execute_grep 测试（路径不存在）**

```python
@patch("agent.tools.grep._check_ripgrep_installed", return_value=True)
def test_execute_grep_path_not_exists(mock_check):
    """测试路径不存在时的错误处理"""
    context = MagicMock(spec=ToolUseContext)
    context.cwd = "/test"
    context.abort_controller.is_aborted = False

    result = execute_grep({"pattern": "TODO", "path": "/nonexistent"}, context)
    assert result.is_error is True
    assert "不存在" in result.output
```

- [ ] **Step 6: 运行测试确认失败**

Run: `python -m pytest tests/tools/test_grep.py::test_execute_grep_path_not_exists -v`
Expected: FAIL (逻辑未实现)

- [ ] **Step 7: 实现 execute_grep（路径检查部分）**

```python
def execute_grep(input: dict[str, Any], context: ToolUseContext) -> ToolResult:
    """执行 grep 搜索

    Args:
        input: 工具输入参数
        context: 工具执行上下文

    Returns:
        ToolResult: 搜索结果
    """
    # 检查 ripgrep 是否安装
    if not _check_ripgrep_installed():
        return ToolResult(
            output="ripgrep 未安装。请安装 ripgrep：\n"
                   "  Windows: winget install BurntSushi.ripgrep.MSVC\n"
                   "  Mac: brew install ripgrep\n"
                   "  Linux: sudo apt install ripgrep",
            is_error=True,
        )

    # 检查中断
    if context.abort_controller.is_aborted:
        return ToolResult(output="搜索被取消", is_error=True)

    # 解析参数
    pattern = input.get("pattern")
    path = input.get("path", context.cwd)
    include = input.get("include")
    max_results = input.get("max_results", DEFAULT_MAX_RESULTS)
    case_sensitive = input.get("case_sensitive", False)
    context_lines = input.get("context_lines", 0)

    # 解析路径
    abs_path = _resolve_path(path, context.cwd)

    # 检查路径存在性
    if not os.path.exists(abs_path):
        return ToolResult(output=f"路径不存在: {abs_path}", is_error=True)

    # TODO: 继续实现其他逻辑
    return ToolResult(output="Not implemented yet", is_error=True)
```

- [ ] **Step 8: 运行测试确认通过**

Run: `python -m pytest tests/tools/test_grep.py::test_execute_grep_path_not_exists -v`
Expected: PASS

- [ ] **Step 9: 编写 execute_grep 测试（基本搜索）**

```python
@patch("agent.tools.grep._check_ripgrep_installed", return_value=True)
@patch("agent.tools.grep.subprocess.run")
def test_execute_grep_basic(mock_run, mock_check):
    """测试基本搜索功能"""
    # 模拟 subprocess.run 返回值
    mock_result = MagicMock()
    mock_result.stdout = "src/main.py:15:# TODO: implement this\n"
    mock_result.returncode = 0
    mock_run.return_value = mock_result

    context = MagicMock(spec=ToolUseContext)
    context.cwd = "/test"
    context.abort_controller.is_aborted = False

    result = execute_grep({"pattern": "TODO"}, context)
    assert result.is_error is False
    assert "src/main.py:15:# TODO: implement this" in result.output
```

- [ ] **Step 10: 运行测试确认失败**

Run: `python -m pytest tests/tools/test_grep.py::test_execute_grep_basic -v`
Expected: FAIL (逻辑未实现)

- [ ] **Step 11: 实现 execute_grep（完整逻辑）**

```python
def execute_grep(input: dict[str, Any], context: ToolUseContext) -> ToolResult:
    """执行 grep 搜索

    Args:
        input: 工具输入参数
        context: 工具执行上下文

    Returns:
        ToolResult: 搜索结果
    """
    # 检查 ripgrep 是否安装
    if not _check_ripgrep_installed():
        return ToolResult(
            output="ripgrep 未安装。请安装 ripgrep：\n"
                   "  Windows: winget install BurntSushi.ripgrep.MSVC\n"
                   "  Mac: brew install ripgrep\n"
                   "  Linux: sudo apt install ripgrep",
            is_error=True,
        )

    # 检查中断
    if context.abort_controller.is_aborted:
        return ToolResult(output="搜索被取消", is_error=True)

    # 解析参数
    pattern = input.get("pattern")
    path = input.get("path", context.cwd)
    include = input.get("include")
    max_results = input.get("max_results", DEFAULT_MAX_RESULTS)
    case_sensitive = input.get("case_sensitive", False)
    context_lines = input.get("context_lines", 0)

    # 解析路径
    abs_path = _resolve_path(path, context.cwd)

    # 检查路径存在性
    if not os.path.exists(abs_path):
        return ToolResult(output=f"路径不存在: {abs_path}", is_error=True)

    try:
        # 构造命令
        cmd = _build_rg_command(
            pattern=pattern,
            path=abs_path,
            include=include,
            max_results=max_results,
            case_sensitive=case_sensitive,
            context_lines=context_lines,
        )

        # 执行命令
        result = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=30,
        )

        # 处理结果
        if result.returncode == 0:
            # 有匹配结果
            results = _parse_rg_output(result.stdout)
            if not results:
                return ToolResult(output="No matches found", is_error=False)

            # 格式化输出
            output_lines = []
            for r in results:
                output_lines.append(f"{r['file']}:{r['line']}:{r['content']}")
            return ToolResult(output="\n".join(output_lines), is_error=False)

        elif result.returncode == 1:
            # 无匹配结果
            return ToolResult(output="No matches found", is_error=False)

        else:
            # 错误
            return ToolResult(
                output=f"搜索失败: {result.stderr}",
                is_error=True,
            )

    except subprocess.TimeoutExpired:
        return ToolResult(output="搜索超时（30秒）", is_error=True)
    except Exception as e:
        return ToolResult(output=f"搜索失败: {str(e)}", is_error=True)
```

- [ ] **Step 12: 运行测试确认通过**

Run: `python -m pytest tests/tools/test_grep.py::test_execute_grep_basic -v`
Expected: PASS

- [ ] **Step 13: 提交**

```bash
git add src/agent/tools/grep.py tests/tools/test_grep.py
git commit -m "feat(F08): implement execute_grep core logic"
```

---

### Task 4: 实现 validate_grep_input

**Files:**
- Modify: `src/agent/tools/grep.py`
- Modify: `tests/tools/test_grep.py`

**Interfaces:**
- Consumes: `_resolve_path` from Task 1
- Produces: `validate_grep_input`

- [ ] **Step 1: 编写 validate_grep_input 测试**

```python
def test_validate_grep_input_valid():
    """测试有效的输入验证"""
    context = MagicMock(spec=ToolUseContext)
    context.cwd = "/test"

    result = validate_grep_input({"pattern": "TODO"}, context)
    assert result.is_valid is True


def test_validate_grep_input_empty_pattern():
    """测试空模式的输入验证"""
    context = MagicMock(spec=ToolUseContext)
    context.cwd = "/test"

    result = validate_grep_input({"pattern": ""}, context)
    assert result.is_valid is False
    assert "pattern" in result.error_message


def test_validate_grep_input_missing_pattern():
    """测试缺少模式的输入验证"""
    context = MagicMock(spec=ToolUseContext)
    context.cwd = "/test"

    result = validate_grep_input({}, context)
    assert result.is_valid is False
    assert "pattern" in result.error_message


def test_validate_grep_input_invalid_max_results():
    """测试无效的 max_results"""
    context = MagicMock(spec=ToolUseContext)
    context.cwd = "/test"

    result = validate_grep_input({"pattern": "TODO", "max_results": -1}, context)
    assert result.is_valid is False
    assert "max_results" in result.error_message
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/tools/test_grep.py::test_validate_grep_input_valid -v`
Expected: FAIL with "ImportError: cannot import name 'validate_grep_input'"

- [ ] **Step 3: 实现 validate_grep_input**

```python
def validate_grep_input(raw_input: dict[str, Any], context: ToolUseContext) -> ValidationResult:
    """校验 grep 输入

    Args:
        raw_input: 工具输入
        context: 工具执行上下文

    Returns:
        ValidationResult
    """
    # 检查 pattern
    pattern = raw_input.get("pattern")
    if not pattern or not isinstance(pattern, str) or not pattern.strip():
        return ValidationResult.failure("pattern 不能为空")

    # 检查 max_results
    max_results = raw_input.get("max_results", DEFAULT_MAX_RESULTS)
    if not isinstance(max_results, int) or max_results < 1:
        return ValidationResult.failure("max_results 必须是正整数")

    # 检查 context_lines
    context_lines = raw_input.get("context_lines", 0)
    if not isinstance(context_lines, int) or context_lines < 0:
        return ValidationResult.failure("context_lines 必须是非负整数")

    # 检查 path（如果提供）
    path = raw_input.get("path")
    if path:
        if not isinstance(path, str) or not path.strip():
            return ValidationResult.failure("path 不能为空字符串")
        abs_path = _resolve_path(path, context.cwd)
        if not os.path.exists(abs_path):
            return ValidationResult.failure(f"路径不存在: {abs_path}")

    return ValidationResult.success()
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/tools/test_grep.py::test_validate_grep_input_valid -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/agent/tools/grep.py tests/tools/test_grep.py
git commit -m "feat(F08): implement validate_grep_input"
```

---

### Task 5: 创建工具注册和完整测试

**Files:**
- Modify: `src/agent/tools/grep.py`
- Modify: `tests/tools/test_grep.py`
- Modify: `src/agent/tools/__init__.py`

**Interfaces:**
- Consumes: `execute_grep`, `validate_grep_input` from Task 3-4
- Produces: `grep_tool` (ToolImpl)

- [ ] **Step 1: 编写工具注册测试**

```python
def test_grep_tool_registered():
    """测试 grep 工具已注册"""
    from agent.tools.grep import grep_tool
    assert grep_tool.name == "grep"
    assert grep_tool.is_read_only({}) is True
    assert grep_tool.is_concurrency_safe({}) is True
    assert grep_tool.is_destructive({}) is False
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/tools/test_grep.py::test_grep_tool_registered -v`
Expected: FAIL with "ImportError: cannot import name 'grep_tool'"

- [ ] **Step 3: 实现工具注册**

```python
# ============================================================
# 工具注册
# ============================================================

grep_tool = build_tool(
    name="grep",
    description="搜索文件内容。支持正则表达式、文件过滤、路径指定等。",
    parameters=GREP_PARAMETERS,
    execute_fn=execute_grep,
    is_read_only=lambda input: True,
    is_concurrency_safe=lambda input: True,
    validate_input=validate_grep_input,
    get_summary=lambda input: f"Searching for '{input.get('pattern', '')}'",
    get_user_facing_name=lambda input: "Grep",
    get_activity_description=lambda input: f"Searching for '{input.get('pattern', '')}'",
)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/tools/test_grep.py::test_grep_tool_registered -v`
Expected: PASS

- [ ] **Step 5: 导出 grep_tool**

```python
# 在 __init__.py 中添加
from agent.tools.grep import grep_tool
```

- [ ] **Step 6: 编写更多测试用例**

```python
@patch("agent.tools.grep._check_ripgrep_installed", return_value=True)
@patch("agent.tools.grep.subprocess.run")
def test_execute_grep_with_include(mock_run, mock_check):
    """测试带文件过滤的搜索"""
    mock_result = MagicMock()
    mock_result.stdout = "src/main.py:15:# TODO: implement this\n"
    mock_result.returncode = 0
    mock_run.return_value = mock_result

    context = MagicMock(spec=ToolUseContext)
    context.cwd = "/test"
    context.abort_controller.is_aborted = False

    result = execute_grep({"pattern": "TODO", "include": "*.py"}, context)
    assert result.is_error is False
    # 验证命令中包含 --glob *.py
    call_args = mock_run.call_args[0][0]
    assert "--glob" in call_args
    assert "*.py" in call_args


@patch("agent.tools.grep._check_ripgrep_installed", return_value=True)
@patch("agent.tools.grep.subprocess.run")
def test_execute_grep_case_sensitive(mock_run, mock_check):
    """测试大小写敏感搜索"""
    mock_result = MagicMock()
    mock_result.stdout = "src/main.py:15:# TODO: implement this\n"
    mock_result.returncode = 0
    mock_run.return_value = mock_result

    context = MagicMock(spec=ToolUseContext)
    context.cwd = "/test"
    context.abort_controller.is_aborted = False

    result = execute_grep({"pattern": "TODO", "case_sensitive": True}, context)
    assert result.is_error is False
    # 验证命令中包含 --case-sensitive
    call_args = mock_run.call_args[0][0]
    assert "--case-sensitive" in call_args


@patch("agent.tools.grep._check_ripgrep_installed", return_value=True)
@patch("agent.tools.grep.subprocess.run")
def test_execute_grep_no_matches(mock_run, mock_check):
    """测试无匹配结果"""
    mock_result = MagicMock()
    mock_result.stdout = ""
    mock_result.returncode = 1
    mock_run.return_value = mock_result

    context = MagicMock(spec=ToolUseContext)
    context.cwd = "/test"
    context.abort_controller.is_aborted = False

    result = execute_grep({"pattern": "NONEXISTENT"}, context)
    assert result.is_error is False
    assert "No matches found" in result.output
```

- [ ] **Step 7: 运行所有测试**

Run: `python -m pytest tests/tools/test_grep.py -v`
Expected: ALL PASS

- [ ] **Step 8: 提交**

```bash
git add src/agent/tools/grep.py tests/tools/test_grep.py src/agent/tools/__init__.py
git commit -m "feat(F08): register grep_tool and add comprehensive tests"
```

---

### Task 6: 创建 spec 文件和最终验证

**Files:**
- Create: `specs/F08-grep-tool.md`
- Modify: `feature_list.json`

**Interfaces:**
- Consumes: 设计文档 `docs/superpowers/specs/2026-06-21-grep-tool-design.md`
- Produces: spec 文件，更新 feature_list.json

- [ ] **Step 1: 创建 spec 文件**

从设计文档复制内容到 `specs/F08-grep-tool.md`，并调整格式为项目 spec 文件风格。

- [ ] **Step 2: 更新 feature_list.json**

```json
{
  "id": "F08",
  "name": "搜索工具",
  "file": "tools/grep.py",
  "spec": "specs/F08-grep-tool.md",
  "status": "done",
  "description": "搜索文件内容（正则、文件过滤、路径指定、大小写控制、上下文显示）"
}
```

- [ ] **Step 3: 运行完整验证**

Run: `make check`
Expected: ALL PASS

- [ ] **Step 4: 提交**

```bash
git add specs/F08-grep-tool.md feature_list.json
git commit -m "docs(F08): mark F08 as done, add spec"
```

---

## 自审清单

### 1. Spec 覆盖

- [x] 内容搜索：Task 3（execute_grep）
- [x] 文件过滤：Task 2（_build_rg_command with include）
- [x] 路径指定：Task 3（path 参数处理）
- [x] 结果控制：Task 2（max_results 参数）
- [x] 大小写控制：Task 2（case_sensitive 参数）
- [x] 上下文显示：Task 2（context_lines 参数）
- [x] 错误处理：Task 3（ripgrep 未安装、路径不存在、超时等）

### 2. Placeholder 扫描

- [x] 无 TBD、TODO 或不完整的部分
- [x] 所有步骤都有实际代码

### 3. 类型一致性

- [x] 函数签名一致
- [x] 参数名称一致
- [x] 返回类型一致

---

## 执行选项

Plan complete and saved to `docs/superpowers/plans/2026-06-21-grep-tool.md`. Two execution options:

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

Which approach?
